# Server Configuration
PORT=5000
CLIENT_URL=http://localhost:3000

# Note: Users will provide their Apify API key through the frontend interface
# No need to store it in environment variables for security reasons
